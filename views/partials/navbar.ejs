<nav>
  <div>
    <ul>
      <li>
        <img src="https://www.knit.ac.in/images/foot-logo.png" alt="Logo" />
      </li>
      <li><a href="/">Home</a></li>
      <li><a href="/company">Drive</a></li>
      
      <% if(currentUser && currentUser.role != 'student'){ %>
      <li class="dropdown">
        <a href="#">Manage</a>
        <div class="dropdown-content">
          <% if(currentUser && currentUser.role == 'Admin'){ %>
          <a href="/manage/TPO">Manage TPO</a>
          <% } %>
          <a href="/manage/placementRecord">Placement Record</a>
        </div>
      </li>
      <% } %>
    </ul>
  </div>



  <div class="d-flex justify-content-between mx-5">
    <% if(currentUser){ %>
      <span style="color: rgb(241, 241, 34);" class="mt-1">
        <a href="/profile/<%=currentUser._id %>">
          <%= currentUser.name %>
        </a>

      </span>
      <form action="/auth/logout">
        <button class="btn btn-outline-danger mx-3 btn-sm">Logout</button>
      </form>
      <% } else { %>
        <form action="/auth">
          <button class="btn btn-danger"> Login/SignUp </button>
        </form>
        <% } %>

  </div>

</nav>